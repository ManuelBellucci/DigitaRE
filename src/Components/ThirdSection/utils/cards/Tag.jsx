const Tag = ({ tagText }) => {
  return (
    <div className='flex px-2 py-1 justify-center items-center uppercase gap-[2px] text-xs text-tag bg-primary rounded font-bold'>{tagText}</div>
  )
}

export default Tag
