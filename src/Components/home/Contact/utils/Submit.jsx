const Submit = ({ onSubmit }) => (
  <button className='bg-transparent hover:bg-primary transition-all ease-in border border-primary text-white hover:text-tag font-bold rounded-lg p-2 w-full mt-4' onClick={onSubmit}>
    Invia
  </button>
)

export default Submit
