const Submit = ({ children }) => {
  return (
    <button className='bg-transparent hover:bg-primary transition-all ease-in border border-primary text-white hover:text-tag font-bold rounded-lg p-2 w-full'>
      Invia
    </button>
  )
}

export default Submit
